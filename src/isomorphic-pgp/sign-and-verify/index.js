const { verifySelfSignature } = require('./verifySelfSignature.js')
const { verify } = require('./verify.js')
const { sign } = require('./sign.js')

module.exports = { verifySelfSignature, verify, sign }
